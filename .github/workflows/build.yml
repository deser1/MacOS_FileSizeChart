name: Build Executables

on:
  push:
    tags:
      - 'v*' # Uruchamia się automatycznie po dodaniu nowego tagu (np. v1.2.0)
  workflow_dispatch: # Pozwala też na ręczne odpalenie z panelu GitHub

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11' # Stabilna wersja dla MacOS

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build macOS App with PyInstaller
        run: |
          # Flaga --windowed tworzy paczkę .app na MacOS
          pyinstaller --noconsole --windowed --name "MacOS_FileSizeChart" macos_filesize_chart.py

      - name: Zip the .app bundle
        run: |
          cd dist
          zip -r MacOS_FileSizeChart_macOS.zip MacOS_FileSizeChart.app

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MacOS_FileSizeChart_macOS
          path: dist/MacOS_FileSizeChart_macOS.zip

  build-windows:
    runs-on: windows-2022
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyinstaller

      - name: Build Windows EXE with PyInstaller
        run: |
          pyinstaller --noconsole --onefile --name "MacOS_FileSizeChart" macos_filesize_chart.py

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: MacOS_FileSizeChart_Windows
          path: dist/MacOS_FileSizeChart.exe
